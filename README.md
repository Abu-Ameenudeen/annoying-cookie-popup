# 🍪 Annoying Cookie Popup

A fun and interactive JavaScript project built as part of the **Scrimba Frontend Developer path**.  
This project simulates an intentionally annoying cookie consent popup to practice DOM manipulation, events, forms, and CSS layout tricks.

## 🚀 Features

- Modal popup triggered using `setTimeout`
- Form handling with `FormData` and `.get()`
- Prevents default form submission behavior
- Dynamic DOM updates using `innerHTML`
- Button disabling & enabling using the `disabled` attribute
- CSS Flexbox trick using `row-reverse` to dodge the Decline button 😄
- Class toggling on hover
- Loading state simulation with delayed messages

## 🧠 Concepts Practiced

- `setTimeout`
- `event.preventDefault()`
- DOM manipulation (`element.style`, `innerHTML`)
- Form handling with `FormData`
- CSS Flexbox (`row-reverse`)
- Toggling classes dynamically
- Handling disabled elements
- Mouse and submit events

## 🛠️ Tech Stack

- HTML5
- CSS3
- Vanilla JavaScript

## 📂 Project Structure

├── index.html
├── index.css
├── index.js
├── images/
│ ├── bugatti.jpg
│ ├── loading.svg
│ └── pirate.gif


## ▶️ How It Works

1. Modal appears automatically after 1.5 seconds
2. Close button is initially disabled
3. Hovering over **Decline** moves the buttons
4. Submitting the form shows a fake loading screen
5. Final message is displayed with the user’s name
6. Close button becomes enabled

## 🎯 What I Learned

- How real cookie consent popups manipulate UI behavior
- Managing user interactions with events
- Creating dynamic UI states without any frameworks
- Writing cleaner, readable vanilla JavaScript

---

💡 *This project was built for learning purposes and humor only.*
